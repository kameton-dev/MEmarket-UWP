<Page
    x:Class="MEmarket_UWP.RepoPropsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MEmarket_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <ScrollViewer Margin="12,0,12,0" FontFamily="Segoe UI">
            <StackPanel>
                <TextBlock Text="URL репозитория" Margin="0,0,0,8"  FontSize="24" FontWeight="SemiLight"/>
                <TextBlock x:Name="RepositoryUrlTextBlock" Text="" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                <TextBlock Text="Создатель" Margin="0,0,0,8"  FontSize="24" FontWeight="SemiLight"/>
                <TextBlock x:Name="CreatorTextBlock" Text="" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                <TextBlock Text="Последнее обновление" Margin="0,0,0,8"  FontSize="24" FontWeight="SemiLight"/>
                <TextBlock x:Name="LastUpdatedTextBlock" Text="" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                <!-- <TextBlock Text="Поддерживаемые платформы:" Style="{ThemeResource HeaderTextBlockStyle}" FontSize="22" Margin="0,0,0,6" Opacity="0.5"/>
                <StackPanel x:Name="SupportedOsPanel" Orientation="Vertical" Margin="0,0,0,16"/> -->
            </StackPanel>
        </ScrollViewer>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Delete" Label="Удалить" Click="DeleteRepoButton_Click"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
