<Page x:Name="pageRoot"
    x:Class="MEmarket_UWP.AppPage"    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MEmarket_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid Grid.Row="1" x:Name="ContentRoot">
            <ScrollViewer>
                <StackPanel Margin="12,12,12,0">
                    <StackPanel Grid.Row="0" Margin="0,0,0,16" Orientation="Horizontal">
                        <Image x:Name="AppIcon" Source="ms-appx:///Assets/StoreLogo.png" Width="80" Height="76" Margin="0,0,0,0" HorizontalAlignment="Left" ImageFailed="IconImage_ImageFailed"/>
                        <StackPanel>
                            <TextBlock x:Name="AppNameText" Text="" Style="{ThemeResource TitleTextBlockStyle}" Margin="8,0,0,0" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
                            <TextBlock x:Name="PublisherText" Text="" Style="{ThemeResource BodyTextBlockStyle}" FontSize="16" Margin="8,0,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <ComboBox x:Name="VersionComboBox" 
                              PlaceholderText="выбрать версию" 
                              Visibility="Collapsed" 
                              SelectionChanged="VersionComboBox_SelectionChanged"
                              DisplayMemberPath="Version"
                              Margin="0,0,0,16"
                              Header="Версия:">
                    </ComboBox>
                    <TextBlock x:Name="AppVersionText" Text="" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,16" FontSize="16"/>

                    <Button x:Name="DownloadButton" Content="Скачать" Click="Download_Click" Margin="0,0,0,8" HorizontalAlignment="Left" Width="150"/>

                    <ProgressBar x:Name="DownloadProgressBar" Minimum="0" Maximum="100" Value="0" Height="5" Margin="0,0,0,8" Visibility="Collapsed"/>
                    
                    <TextBlock x:Name="SummaryText" Text="" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,16" FontSize="16" FontWeight="Bold"/>

                    <TextBlock Text="Описание" Margin="0,0,0,8"  FontSize="24" FontFamily="Segoe UI Semilight"/>
                    <RichTextBlock x:Name="DescriptionText" Margin="0,0,0,16"/>
                    <HyperlinkButton x:Name="ToggleDescriptionButton" Content="Развернуть" HorizontalAlignment="Left" Click="ToggleDescriptionButton_Click" Margin="0,0,0,16" Visibility="Collapsed"/>

                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,16" Visibility="Collapsed" x:Name="ScreenshotsScrollViewer">
                        <ListView x:Name="ScreenshotsListView" Margin="0,0,0,0">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding}" Width="225" Height="325" Margin="0,0,8,0" Stretch="UniformToFill" Tapped="Screenshot_Tapped" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </ScrollViewer>

                    <TextBlock Text="Подробности" Margin="0,0,0,8"  FontSize="24" FontFamily="Segoe UI Semilight"/>
                    
                    <!-- <TextBlock x:Name="AppSizeText" Text="Размер: " Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,8" FontSize="16"/> 
                    <TextBlock x:Name="MinVersionText" Text="Мин. версия: " Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,8" FontSize="16"/> -->
                    <TextBlock x:Name="CapabilitiesText" Text="Возможности:" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,8" FontSize="16" Visibility="Collapsed"/>
                    <ItemsControl x:Name="CapabilitiesList" Margin="0,0,0,16" Visibility="Collapsed">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <TextBlock Text="• " Style="{ThemeResource BodyTextBlockStyle}"/>
                                    <TextBlock Text="{Binding}" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock x:Name="AppTypeText" Text="Тип приложения: " Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,16" FontSize="16" Visibility="Collapsed"/>
                    
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
